name: Fetch Typhoon Data

on:
  schedule:
    - cron: '0 * * * *' # 每小時自動執行一次
  workflow_dispatch: # 允許手動強制執行

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Fetch data from CWA
        run: |
          curl -s "https://opendata.cwa.gov.tw/fileapi/v1/opendataapi/W-C0034-002?Authorization=${{ secrets.CWA_API_KEY }}&format=JSON" -o typhoon.json

      - name: Commit and push changes
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add typhoon.json
          git commit -m "自動更新颱風最新路徑資料" || exit 0
          git push
